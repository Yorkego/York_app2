<% if current_user.liked? @post %>
  $('.like-btn').addClass('liked')
<% else %>
  $('.like-btn').addClass('.like-btn')
<% end %>
$('#<%= @post.id %>.likes-count').html('<%= @post.get_upvotes.size %>')
