require "rails_helper"

RSpec.describe "posts/show.html.erb", type: :view do

  # it "displays one post" do
  #   user = create(:user)

  #   @post = user.posts.create(attributes_for(:post, title: "slicer"))

  #   render(file: 'posts/show.html.erb', assigns: { post: @post })



  #   expect(rendered).to match /slicer/
  # end
end
