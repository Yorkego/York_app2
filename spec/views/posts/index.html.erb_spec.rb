require "rails_helper"

RSpec.describe "posts/index.html.erb", type: :view do
  # it "displays all posts" do
  #   user = create(:user)
  #   user.posts.create(attributes_for(:post, title: "slicer"))
  #   user.posts.create(attributes_for(:post, title: "dicer"))

  #   # @posts = Post.all.paginate(page: params[:page], per_page: 5)

  #   # assign(:posts, [
  #   #     user.posts.create!(attributes_for(:post, title: "slicer")),
  #   #     user.posts.create!(attributes_for(:post, title: "dicer"))
  #   # ])
  #   assign(:posts, [stub_model(Post, title: "slicer"), stub_model(Post, title: "dicer")])

  #   render
  #   expect(rendered).to match /slicer/
  #   expect(rendered).to match /dicer/
  # end
end
